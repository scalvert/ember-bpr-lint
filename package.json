{
  "name": "eslint-plugin-ember-bpr-lint",
  "version": "0.2.4",
  "description": "Eslint rules for linting BPR specific patterns.",
  "main": "lib/index.js",
  "directories": {
    "doc": "doc",
    "test": "tests"
  },
  "scripts": {
    "test": "mocha --recursive tests",
    "test-server": "mocha --recursive --growl --watch tests"
  },
  "repository": "",
  "engines": {
    "node": ">= 6.0.0"
  },
  "author": "",
  "license": "MIT",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "devDependencies": {
    "eslint": "^4.19.1",
    "mocha": "^5.1.0",
    "mocha-eslint": "^4.1.0"
  },
  "dependencies": {
    "requireindex": "^1.1.0"
  }
}
